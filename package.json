{
  "name": "alrevolutum-website",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "dev:cloudflare": "npx astro build && npx wrangler dev",
    "deploy:cloudflare": "npx astro build && npx wrangler deploy",
    "deploy:preview": "npx astro build && npx wrangler versions upload",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.10",
    "@astrojs/rss": "^4.0.13",
    "@astrojs/sitemap": "^3.6.0",
    "astro": "^5.15.1",
    "sharp": "^0.34.2"
  },
  "devDependencies": {
    "wrangler": "^4.45.0"
  }
}
